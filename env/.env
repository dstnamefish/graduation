# 基础环境变量（所有环境共享）
# 注意：变量名必须以 VUE_APP_ 或 REACT_APP_ 开头（根据框架要求），否则无法被框架识别

# 应用名称（通用配置）
VITE_APP_NAME = "医疗后台管理"
# 应用版本（通用配置）
VITE_APP_VERSION = "1.0.0"
# 接口协议（http/https，通用默认值）
VITE_APP_API_PROTOCOL = "http"
# 是否开启调试模式（默认关闭，可在特定环境覆盖）
VITE_APP_DEBUG = false