# 生产环境变量（仅上线部署时生效）
NODE_ENV = "production"  # 标识当前环境为生产环境

# 生产环境接口域名（正式服务器）
VITE_APP_API_DOMAIN = "api.example.com"

# 完整接口基础路径（生产环境通常用https）
VITE_APP_API_PROTOCOL = "https"  # 覆盖.env中的http
VITE_APP_API_BASE_URL = "${VITE_APP_API_PROTOCOL}://${VITE_APP_API_DOMAIN}"

# 生产环境静态资源路径（CDN地址）
VITE_APP_STATIC_URL = "https://cdn.example.com/static"

# 生产环境日志级别（精简，避免暴露信息）
VITE_APP_LOG_LEVEL = "error"

# 生产环境是否启用监控（如Sentry）
VITE_APP_MONITOR_ENABLE = true
VITE_APP_MONITOR_DSN = "https://xxx@sentry.example.com/1"