#!/usr/bin/env sh
. "$(dirname "$0")/h"

# 运行 lint-staged（推荐，配合 lint-staged 使用）
npx lint-staged
if [ $? -ne 0 ]; then
  echo "lint-staged 检查未通过，终止 commit。"
  exit 1
fi

# 你也可以直接运行 lint 或格式化命令
# npm run lint
# npm run format