/**
 * @description 表格功能配置
 * @property {boolean} isZebra - 是否显示斑马纹
 * @property {boolean} isBorder - 是否显示边框
 * @property {boolean} isHeaderBackground - 是否显示表头背景
 */
export interface TableConfig {
  isZebra: boolean;
  isBorder: boolean;
  isHeaderBackground: boolean;
}